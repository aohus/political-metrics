environment: development
etl_paths:
  data_root: data
  assembly:
    base: assembly
    temp_formatted: assembly/temp/formatted
    temp_raw: assembly/temp/raw
    ref: assembly/ref
    raw: assembly/raw
    formatted: assembly/formatted
  document:
    json: document/formatted
    pdf: document/pdf
  law:
    raw: law/raw
